// Adresse des Backends (Node-Server mit der API und der gespeicherten Liste).
// Leer lassen ("") wenn Frontend und Backend vom selben Server ausgeliefert werden
// (z.B. lokal mit `npm start`). Bei getrenntem Hosting (z.B. dieses Frontend auf
// GitHub Pages, Backend auf einem eigenen Server) hier die volle Adresse eintragen,
// z.B. "https://tt.meinserver.de/api".
window.RANGBESTIE_CONFIG = {
  apiBase: "https://tischtennis.oxibuff.at/api",
};
